name: APK Builder

on:
  push:
    branches:
      - master
  pull_request:
    branches:
      - master

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: LibertyPhoneProject/Push-APK@v2
      with:
        apk-name: net.sourceforge.opencamera.apk
        desitnation-folder: Camera
        

