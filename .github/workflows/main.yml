name: APK Builder

on:
  push:
    branches:
      - master
  pull_request:
    branches:
      - master

jobs:
  build:
    runs-on: Alpine
    steps:
    - uses: LibertyPhoneProject/Push-APK@v1
      with:
        apk-name: net.sourceforge.opencamera.apk
        desitnation-folder: Camera
        

